Permite filtrar resultados de una consulta SQL, especificando la condición que deben cumplir las filas.

---

```
SELECT nombre, email
FROM clientes
WHERE ciudad = 'Madrid';
```

Estos operadores se pueden ocupar con WHERE

 ```
- = equal to
- != not equal to
- > greater than
- < less than
- >= greater than or equal to
- <= less than or equal to
```
