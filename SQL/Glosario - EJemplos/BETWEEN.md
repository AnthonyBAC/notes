1.  **Se utiliza como filtro dentro del [[WHERE]] para ver el resultados entre un rango**
	
	- Acepta dos valores, que pueden ser texto, números o fechas.

```
SELECT *
FROM movies
WHERE year BETWEEN 1990 AND 1999;
```
