Retornan el valor mínimo o máximo  de una columna.

---

1. **Ejemplo MAX**
 ```
 SELECT MAX(price)
 FROM fake_apps;
 ``` 

	Muestra la aplicación con el mayor valor.

---

2. **Ejemplo MIN**
 ```
 SELECT MIN(downloads)
 FROM fake_apps;
 ``` 

	Muestra la aplicación con menor descargas.